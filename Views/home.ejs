<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> </title>
</head>
<body>
    <h1> <%= pageHeader %> </h1>
    <div>
         <% data.map((emp,index)=>{ %>
        <div>
          
            <a href="/emp/<%= index + 1%>">
                Click for Details of <%=emp.name %> 
              </a>
              
        </div>
    </div>

   <% }) %> 
   <br>
   <hr>
   <% links.map(endPoints =>{ %>
      <a href = "<%= endPoints.link %> "> Click here for <%= endPoints.name %>  </a> <br>
  <% }) %>
  
</body>
</html>